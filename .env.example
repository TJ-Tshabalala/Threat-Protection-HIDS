# Example environment variables for running the server and agent
# Generate a strong random API key for production and set it here or in your environment.
API_KEY=replace-with-a-secure-random-string
OLLAMA_URL=http://localhost:11434
# Comma-separated origins for CORS (optional)
ALLOWED_ORIGINS=
# Maximum payload size allowed (bytes)
MAX_PAYLOAD_BYTES=204800
